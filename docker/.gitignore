# Debug files generated by Splunk TA Addon
**/debug*.sh*
.DS_Store
**/*.retry

# .venv is created by pipenv. It needs to be in a relative path for VScode to use and install modules.
# You can enable using the same dir by setting the env var PIPENV_VENV_IN_PROJECT=1
.venv/

vault_pass.txt
local/
.vscode/
id_rsa
id_rsa.pub
